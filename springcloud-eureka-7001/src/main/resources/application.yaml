server:
  port: 7001


eureka:
  instance:
    hostname: eureka7001.com
  client:
    service-url:
      defaultZone: http://eureka7002.com:7002/eureka/

    register-with-eureka: false #不在注册中心注册自己
    fetch-registry: false #值为false 表示自己为注册中心